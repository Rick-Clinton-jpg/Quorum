# Dependencies for Quorum-owned gate/ code (quorum_gate.py, quorum_paths.py,
# firestore_audit.py, firestore_intent.py) - kept separate from
# gate/requirements.txt, which is the vendored Trust Boundary repo's own
# file (see gate/CONTRIBUTING.md's governance boundary on that file).

google-cloud-firestore>=2.14.0
numpy

# gate/github_action.py's one direct REST call (PR creation) - git itself
# handles the clone/branch/apply/commit/push via subprocess, no library
# needed for that part.
requests

# gate/otel_tracing.py - exports each gate stage as a real OTel span to
# Cloud Trace (falls back to console export if Cloud Trace isn't
# reachable - see that module's docstring).
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-gcp-trace>=1.6.0
